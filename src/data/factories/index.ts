/**
 * Data Factory Infrastructure — Barrel Export
 *
 * Exports shared factory utilities and types.
 * Team-specific factories live in teams/{TEAM}/test-data/factories/.
 */
export { withDefaults } from './types';
export type { FactoryOverride, LegionTime, DayIndex } from './types';
